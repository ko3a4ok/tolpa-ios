import React, { Component } from 'react';
import {
  AsyncStorage,
  AppRegistry,
  StyleSheet,
  Text,
  View,
  Button,
  Navigator,
  TouchableHighlight,
} from 'react-native';


export default class MainScreen extends Component {

  render() {
    return (
      <View style={{backgroundColor: 'red', flex:1, top: 50, padding: 20}}>
      </View>
    );
  }
}

const styles = StyleSheet.create({
});
